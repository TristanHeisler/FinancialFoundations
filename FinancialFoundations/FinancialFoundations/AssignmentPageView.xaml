<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FinancialFoundations"
             x:Class="FinancialFoundations.AssignmentPageView">

    <StackLayout>
        <Label Text="Assignment"/>
        <ListView HasUnevenRows="True" ItemsSource="{Binding MultipleChoiceQuestionCollection}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell IsEnabled="false">
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding QuestionText}"/>
                            <Label HorizontalTextAlignment="Center" Text="{Binding AnswerCollection[0].AnswerText}" VerticalTextAlignment="Center"/>
                            <Label HorizontalTextAlignment="Center" Text="{Binding AnswerCollection[1].AnswerText}" VerticalTextAlignment="Center"/>
                            <Label HorizontalTextAlignment="Center" Text="{Binding AnswerCollection[2].AnswerText}" VerticalTextAlignment="Center"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <Button Text="Submit" Clicked="Button_OnClicked" />
    </StackLayout>

</ContentPage>
